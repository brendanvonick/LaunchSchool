['ant', 'bear', 'caterpillar'].pop().length

// 11

/*  This is the length value of the last element in the original array,
    'caterpillar'. The pop() method removes the last element from the array
    and returns it, and length is being called on that removed element.
*/
